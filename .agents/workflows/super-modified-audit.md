# 🛡️ SUPER-MODIFIED MASTER AUDIT & EVALUATION WORKFLOW (`/super-modified-audit`)

> **Command Identifier**: `/super-modified-audit`  
> **Target Applicability**: Enterprise Software Factory (All Standalone Products: `Lead-suite Pro` / `GHL-PULSE`, `EMS`, `omnistock`, `LexAI`)  
> **Authoritative Theme Standard**: `⚡ Zenitsu Thunder Gold` (`#F9E006` Vivid Cyber Gold / `#0B1C30` Solid Card Interior)  
> **Governance Spec**: Demon Slayer Cyber Glass Design Tokens & 22-Phase Master Audit Protocol  

---

## 📌 WORKFLOW EXECUTION OVERVIEW

When the user types `/super-modified-audit`, the Antigravity Master Orchestrator activates this 22-Phase Super-Modified Audit Engine.

```mermaid
graph TD
    Trigger[/super-modified-audit] --> Phase1[1. Ultimate 8-Stage Remediation Cycle]
    Phase1 --> Phase2[2. server.ts Zod Validation & Security]
    Phase2 --> Phase3[3. CI Gating & SAST/DAST CodeQL]
    Phase3 --> Phase4[4. Playwright E2E & Axe A11y]
    Phase4 --> Phase5[5. Observability & k6 Load Tests]
    Phase5 --> Phase6[6. 5-Anchor Pixel & Code Guarantee]
    Phase6 --> Phase7[7. Agentic Auditor Swarm Inspection]
    Phase7 --> Phase8[8. Zenitsu Thunder Gold Theme Audit]
    Phase8 --> Phase9[9. 30+ GHLA Components & Modals Sweep]
    Phase9 --> Phase10[10. 72-Brain Swarm Verdict]
    Phase10 --> Phase11[11. 100% PASS Thermal Attestation]
```

---

## 📋 MANDATORY 22-PHASE COMPLIANCE CHECKLIST

### 🔄 1. The Ultimate 8-Stage Remediation & Prevention Cycle
- **Stage 1 (Identify)**: Immediately record every defect in `master_component_checklist.md` BEFORE code edits.
- **Stage 2 (RCA)**: Perform deep root cause analysis.
- **Stage 3 (Plan)**: Formulate fix in Taglish and obtain explicit user signal (`TOKEN-LOCK-EXPLICIT-PERMIT`).
- **Stage 4 (Resolve)**: Execute atomic code edits (max 3 files per turn).
- **Stage 5 (Confirm)**: Dual validation (DevTools visual screenshot + CLI script output).
- **Stage 6 (Prevent)**: Cross-product rule generalization.
- **Stage 7 (Script)**: Codify check in `master_project_audit.py` & `ast-scan.ts`.
- **Stage 8 (Document)**: Update `company_master_design_tokens_spec.md` and mark `[x] RESOLVED`.

---

### 🛡️ 2. Express Backend & Repository Technical Controls
- **`server.ts` Zod AI Schema**: Wrap raw `JSON.parse` in Zod `schema.safeParse` with redacted audit receipts.
- **Rate Limiting**: Apply `express-rate-limit` on all `/api/*` endpoints.
- **File Manifest**: Generate `audits/manifest.json` with sha256 checksums for 100% of tracked source files.
- **AST Sweep**: Run `npx tsx scripts/ast-scan.ts`.

---

### 🚀 3. CI Gating, SAST/DAST & Supply-Chain
- Execute `.github/workflows/ci.yml` (`tsc --noEmit`, ESLint, CodeQL SAST, Dependabot, SBOM).

---

### 🎭 4. Playwright E2E Smoke, Visual Diffs & Axe A11y
- Run `tests/e2e/smokeflow.spec.ts` (`Register` ➔ `Login` ➔ `Dashboard` ➔ `SpeedToLead` ➔ `Export PDF`).
- Verify Axe A11y zero WCAG AA violations & keyboard navigation.

---

### ⚡ 5. `⚡ Zenitsu Thunder Gold` Visual Signature
- Lock `#F9E006` Vivid Cyber Gold base accents on `#0B1C30` solid dark navy card surfaces.
- Enforce `STRICT-SOLID-INTERIOR-RULE` and `SCROLL-CYBER-GLASS`.

---

### 🧠 6. 72-Brain Swarm & Agentic Auditor Swarm
- Execute `python .agents/scripts/council_debate.py` across DeepSeek-R1, Qwen 2.5 Coder, GPT-4o, and Llama 3.3.
- Run `COPILOT-01` micro-to-macro code inspection & `Saccade UI/UX` visual evaluation.

---

### 📝 7. Final Master Audit Ledger Lock & Thermal Attestation
- Run `python .agents/scripts/master_project_audit.py` to attain **100.0% PASS SCORE (47/47 Checks Passed)**.
