# SYSTEM MEMORY: Architecture State
> **Synthesized Memory Compaction Protocol:** This file is continuously updated by the orchestrator to prevent state degradation. It acts as the "working memory" for the Claude 5.0 cognitive model.

## 1. Active Workspace
**Root:** `c:\Users\Admin\.antigravity-ide\`
**Current Active Project:** `Lead-suite-Pro` (with `OmniStock` and `EMS` as locked reference codebases).

## 2. Core Architectural Scaffold
- **Frontend Engine:** Vite + React + TypeScript + TailwindCSS
- **State Management:** (Pending exact extraction from Lead-suite-Pro)
- **Backend / DB Integration:** Firebase (Google OAuth via Console)
- **Design System:** Demon Slayer Ukiyo-e Cyber Glass (Stitch MCP `assets/1640102745724511064`)

## 3. Current Execution Context
- **Goal:** Execute Firebase Google OAuth Test on the Desktop App.
- **Status:** The user is currently in the Firebase Console (https://console.firebase.google.com/) fetching the `firebaseConfig` block to paste into the Desktop App setup window.

## 4. Hard Constraints & Anomalies
- **Zero-Mock Data Law is ACTIVE:** No static arrays allowed.
- **Claude 5.0 Override is ACTIVE:** RLAIF, Micro-OCR, and Agentic Workflow enforced globally.
- **Codebase Hard Lock:** OmniStock and EMS folders are strictly read-only reference benchmarks.

*(This file must be read at the start of any complex planning phase to ensure the Orchestrator does not lose context of the overarching system state).*
