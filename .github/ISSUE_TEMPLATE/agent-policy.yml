name: Add / Update Agent Policy
about: Draft or update agent policy and runtime enforcement
title: "[POLICY] Agent policy update"
labels: ["policy","security"]
body:
  - type: markdown
    attributes:
      value: |
        Please describe the policy change and include the YAML snippet. Include tests and CI changes.
  - type: input
    attributes:
      label: "Policy file path"
      placeholder: "policies/agent_policy.yaml"
